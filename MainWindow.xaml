<Window x:Class="Progect1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Progect1"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                <StackPanel Orientation="Vertical" Margin="0 0 5 0">
                    <TextBlock x:Name="lb_login" Text="Логин"/>
                    <TextBlock x:Name="lb_password"  Text="Пароль"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" Width="250">
                    <TextBox x:Name="tb_login" Height="{Binding ElementName=log,Path=Text, Mode=TwoWay}"/>
                    <TextBox x:Name="tb_password" Height="{Binding ElementName=log,Path=Text, Mode=TwoWay}"/>
                </StackPanel>
            </StackPanel>
            <Button Content="OK" x:Name="bt_OK" Click="bt_OK_Click"/>
        </StackPanel>

    </Grid>
</Window>
